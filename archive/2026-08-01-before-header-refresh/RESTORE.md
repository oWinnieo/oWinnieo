# Header backup

This directory contains the exact `assets/header.svg` used immediately before the 2026-08-01 header redesign.

To restore it, replace `assets/header.svg` with `archive/2026-08-01-before-header-refresh/assets/header.svg`, then commit the change.
